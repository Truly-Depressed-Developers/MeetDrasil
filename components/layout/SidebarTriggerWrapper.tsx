'use client';

import { SidebarTrigger } from '@/components/ui/sidebar';

const SidebarTriggerWrapper = () => {
  return <SidebarTrigger className="p-2 hover:bg-transparent hover:text-inherit" />;
};

export default SidebarTriggerWrapper;
